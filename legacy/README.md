// Legacy Vanilla JS implementation reference
// This folder contains the original HTML/JS before the React migration.
// DO NOT delete. Preserved for reference and git history.

# Maro Dash — Legacy Implementation

This folder contains the original Vanilla HTML + JavaScript implementation.

It is preserved here as a safe reference during and after the React migration.

- `script.js` — Original JavaScript (842 lines)

The full `index.html` is available in git history on the `main` branch.
